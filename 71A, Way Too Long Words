n = int(input())
input_list = []
i = 0

while i < n:
    input_list.append(input())
    i += 1

for word in input_list:
    if len(word) > 10:
        print(f"{word[0]}{len(word) - 2}{word[-1]}")
    else:
        print(word)
